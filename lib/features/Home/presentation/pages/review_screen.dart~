import 'dart:developer';

import 'package:cached_network_image/cached_network_image.dart';
import 'package:flutter/material.dart';
import 'package:flutter_rating_bar/flutter_rating_bar.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';
import 'package:nursy/core/theming/app_colors.dart';
import 'package:nursy/core/widgets/custom_app_bar.dart';

import '../../../../core/theming/app_text_styles.dart';
import '../../../../core/widgets/custom_button.dart';
import '../../../../core/widgets/custom_text_field.dart';
import '../../../../generated/l10n.dart';

class ReviewScreen extends StatelessWidget {
  const ReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: CustomAppBar(
          title: S.of(context).review,
      ),
      body: Padding(
        padding: const EdgeInsets.symmetric(horizontal: 16.0),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          crossAxisAlignment: CrossAxisAlignment.center,
          children: [
            ClipRRect(
              borderRadius: BorderRadius.circular(365.r),
              child: CachedNetworkImage(
                imageUrl:
                "https://images.pexels.com/photos/771742/pexels-photo-771742.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=2",
                height: 80.h,
                width: 80.w,
                fit: BoxFit.cover,
              ),
            ),
            SizedBox(
                height: 12.h),
            Text(
              'Huda Anwar',
              style: AppTextStyles.ibmPlexSans18Bold(
                context,
              ).copyWith(color: AppColors.eerieBlack,
              ),
            ),
            SizedBox(
                height: 8.h),
            RatingBar.builder(
              itemSize: 20,
              initialRating: 3,
              minRating: 1,
              direction: Axis.horizontal,
              allowHalfRating: true,
              itemCount: 5,
              itemPadding: EdgeInsets.symmetric(horizontal: 4.0),
              itemBuilder:
                  (context, _) => Icon(Icons.star, color: Color(0xffEF9B26)),
              onRatingUpdate: (rating) {
                log(rating.toString());
              },
            ),
            SizedBox(
              height: 40.h,
            ),
            Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Text(
                    S.of(context).any_comment,style:
                AppTextStyles.ibmPlexSans16SemiBold(context).copyWith(
                    color: Color(0xff375885),),
                ),
                SizedBox(
                  height: 14.h,
                ),
                CustomTextField(
                  labelText: S.of(context).title,
                  fillColor: AppColors.softGray,
                ),
                SizedBox(
                  height: 16.h,
                ),
                CustomTextField(
                  labelText: S.of(context).any_comment,
                  fillColor: AppColors.softGray,maxLines: 5,

                ),
                SizedBox(
                  height: 28.h,
                ),
               Text(
                 S.of(context).do_you_have_complains,
                 style: AppTextStyles.ibmPlexSans18Bold(context).copyWith(
                   color: Color(0xff375885),
                 ),
               ),
                SizedBox(
                  height: 8.h,
                ),
                Text(
                  'Any Comment for nursy ?',
                  style: AppTextStyles.ibmPlexSans16SemiBold(context).copyWith(
                    color: Color(0xff375885),
                  ),
                ),
                SizedBox(
                  height: 64.h,
                ),
                CustomButton(
                  text: S.of(context).done,
                  xPadding: 77,
                  yPadding: 14,
                  bgColor: AppColors.brightTeal,
                  width: 200.w,
                  textStyle: AppTextStyles.inter18Bold(context).copyWith(
                           color: AppColors.white
                           ),
                  onPressed: (){},
                )
              ],
            )

          ],
        ),
      ),
    );
  }
}
